<?php

// TODO: Check if the user is logged in as a car rental agency and has the necessary permissions

// Get the new car data from the POST request
$model = $_POST['model'];
$number = $_POST['number'];
$capacity = $_POST['capacity'];
